root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  delay = 1000
  exclude_dir = ["tmp", "vendor"]
  include_ext = ["go", "env"]
  exclude_regex = []
  stop_on_error = true

[log]
  time = true

[misc]
  clean_on_exit = true
