services:
  address-quality:
    image: ghcr.io/samaita/address-quality:latest
    container_name: address-quality
    pull_policy: always
    ports:
      - "7300:7300"
    volumes:
      - address-data:/data
    env_file:
      - .env.prod
    restart: unless-stopped

volumes:
  address-data:
